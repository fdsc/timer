1. _setup_copy_menu_for_label нужно переписать через ClipboardMixin
2. Контрольные задачи
4. В поле "осталось" необходимо выводить дни, а не только часы.
5. Диалоги, сообщающие об ошибках лучше вынести в отдельный метод с синхронизацией потоков.


Выводи только функции, которые были изменены или добавлены. Если изменяются большие функции, может выводить изменения только частично (с указанием, где именно эти изменения находятся).


python3 print.py > /inRamA/txt



Уже имеющиеся скрипты



Необходимо:



Выводи только функции, которые были изменены или добавлены. Если изменяются большие функции, можно выводить изменения только частично (с указанием, где именно эти изменения находятся).
Минимум комментариев, если их не было ранее. Не удаляй комментарии, если они были.

